import React from "react";
import { Animated, View, Text } from "react-native";

// 더하기, 빼기, 곱하기, 나누기 또는 모듈로를
export default function AnimatedCombining() {
  return <Animated.Text style={{ fontSize: 60 }}>🍊</Animated.Text>;
}
